# Ключевые бизнес-сущности

> Описание сущностей, с которыми взаимодействует пользователь или внешняя система. Не внутренняя модель данных, а то, что потребитель продукта видит, получает или передаёт.

---

## <Название сущности>

**Что это:** <одно-два предложения о бизнес-смысле сущности — не про реализацию>

**Кто взаимодействует:** <роли пользователей и/или внешние системы>

**Откуда подтверждение:** <путь к DTO / OpenAPI-schema / компоненту фронтенда>

### Поля

| Поле | Тип | Видно пользователю | Только на бэкенде | Доступ по роли | Примечание |
|---|---|---|---|---|---|
| `id` | uuid | да | нет | все роли | первичный ключ |
| `status` | enum | да | нет | все роли | значения: `active`, `inactive`, `pending` |
| `internal_score` | float | нет | **да** | — | вычисляется ML-моделью, не выгружается в API |
| `created_by_id` | uuid | нет | **да** | — | FK на users, в API возвращается `created_by_name` |
| `deleted_at` | timestamp | нет | **да** | — | soft delete, скрыт от API-ответа |

> Колонка **«Только на бэкенде»** = поле существует в БД или внутренней логике, но **не возвращается** пользователю или внешней системе ни в каком API-ответе.

---

## <Вторая сущность>

**Что это:** ...

**Кто взаимодействует:** ...

**Откуда подтверждение:** ...

### Поля

| Поле | Тип | Видно пользователю | Только на бэкенде | Доступ по роли | Примечание |
|---|---|---|---|---|---|
| ... | ... | ... | ... | ... | ... |

---

## Расхождения и открытые вопросы

| Сущность | Поле | Расхождение | Статус |
|---|---|---|---|
| `Order` | `payment_status` | в Swagger поле не задокументировано, но фронтенд его использует | требует подтверждения |

---

## Источники подтверждения

| Сущность | Источник (фронтенд) | Источник (бэкенд) |
|---|---|---|
| `<Название>` | `<repo>/src/types/entity.ts:12` | `<repo>/app/schemas/entity.py:34` |
