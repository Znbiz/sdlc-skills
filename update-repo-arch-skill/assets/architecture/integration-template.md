# Интеграции сервиса: <название сервиса>

Один файл описывает один сервис и все интеграции, которые входят в него и выходят из него.
Имя файла лучше задавать по имени сервиса, например `billing-api.md` или `order-service.md`.

## Краткое описание сервиса

- Сервис: `<название>`
- Репозиторий: `<если известен>`
- Роль в системе: `<кратко>`
- Уровень достоверности: `<высокий|средний|низкий>`

## Входящие интеграции

### `<сервис-источник>-><текущий-сервис>`

- Тип: `<REST|gRPC|Kafka|SFTP|Webhook|...>`
- Источник: `<кто вызывает или публикует>`
- Назначение: `<для чего нужна интеграция>`
- Ключевые данные: `<основные сущности, сообщения, payload>`
- Надежность: `<таймауты, ретраи, идемпотентность, DLQ, обработка ошибок>`
- Подтверждающие артефакты:
  - `<файл, конфигурация или другой артефакт>`
- Открытые вопросы: фиксируй в `open-questions.md`, колонка «Контекст»: `интеграция: <имя этой интеграции>`

## Исходящие интеграции

### `<текущий-сервис>-><сервис-назначение>`

- Тип: `<REST|gRPC|Kafka|SFTP|SDK|DB link|...>`
- Цель: `<какую систему или сервис вызывает>`
- Назначение: `<для чего нужна интеграция>`
- Ключевые данные: `<основные сущности, сообщения, payload>`
- Надежность: `<таймауты, ретраи, circuit breaker, fallback, DLQ>`
- Подтверждающие артефакты:
  - `<файл, конфигурация или другой артефакт>`
- Открытые вопросы: фиксируй в `open-questions.md`, колонка «Контекст»: `интеграция: <имя этой интеграции>`

## Сводка по данным и границам доверия

- Аутентификация и авторизация: `<если известно>`
- Сетевые или инфраструктурные границы: `<если видно в коде или конфигурации>`
- Наблюдаемость: `<логи, метрики, трейсы, аудит>`

## Итоговые выводы

- `<какие интеграции критичны>`
- `<что подтверждено хорошо, а что требует уточнения>`
