# Вспомогательные репозитории

Реестр репозиториев категории `support` (`library`, `test-repo`, `infra`): они обслуживают продуктовые сервисы, но сами не реализуют продуктовую логику. Не дублируй эти записи в `landscape.yaml` и `features-index.md` — это разные срезы документации.

## `<repo-name>`

- **Тип**: `<library|test-repo|infra>`
- **Репозиторий**: `<url-репозитория>`
- **Роль**: `<краткое назначение>`
- **Обслуживает**: `<список сервисов/доменов, к которым относится>`
- **Ключевые технологии**: `<основной стек, см. также architecture/tech-stack.md>`
- **Для `test-repo`**: `<какие сервисы и потоки покрывают тесты, тип тестов (e2e/integration/acceptance)>`
- **Для `infra`**: `<что провижинит — окружения, сервисы, инфраструктурные компоненты>`
- **Для `library`**: `<кто из продуктовых сервисов её использует, публичный контракт если есть>`
- **Источник подтверждения**: `<README / конфиг / код>`
