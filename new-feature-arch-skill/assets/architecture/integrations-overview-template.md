# Обзор интеграций изменения

## Границы описания

- Продукт или система: `<название>`
- Изменение или фича: `<название>`
- Уровень детализации: `<solution-design|change-impact|rollout>`

## Общая картина

<кратко опиши, какие интеграции появляются, меняются или остаются совместимыми и почему это важно для решения>

## Карта интеграций

| Интеграция | Тип | Change Type | Направление | Исходный компонент | Целевая система | Назначение | Совместимость | Детали |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `<название>` | `<REST|gRPC|Kafka|SFTP|SDK|...>` | `<new|modified|unchanged-critical|deprecated>` | `<входящая|исходящая|двусторонняя>` | `<сервис или джоба>` | `<система>` | `<для чего нужна>` | `<полная|частичная|ломающая>` | `[ссылка](./<файл>.md)` |

## Разрез по направлению

### Входящие

- `<кто вызывает продукт и что меняется для него>`

### Исходящие

- `<какие внешние системы вызывает продукт и что меняется>`

### Асинхронные

- `<брокеры, топики, очереди, вебхуки и событийные потоки>`

## Основные потоки данных

1. `<поток: источник -> продукт -> получатель, с ключевыми сущностями или событиями>`

## Совместимость и миграция

- `<как будут жить старые и новые взаимодействия в переходный период>`
- `<какие версии payload или endpoint поддерживаются одновременно>`

## Надежность

- `<таймауты, повторы, DLQ, идемпотентность, fallback, circuit breaker>`

## Наблюдаемость

- `<логи, метрики, трейсы, аудит или другие сигналы для контроля rollout>`

## Риски

1. `<что может пойти не так при реализации или rollout>`

<!-- Открытые вопросы по интеграциям → open-questions.md, колонка «Контекст»: integrations-overview -->

## Подтверждающие артефакты

- `<текущий код, контракты, схемы, RFC, HLD и другие источники>`
