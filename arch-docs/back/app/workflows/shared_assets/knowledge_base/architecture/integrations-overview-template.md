# Обзор интеграций

## Границы описания

- Продукт или система: `<название>`
- Границы анализа: `<репозитории, сервисы, контур или среда при необходимости>`

## Карта интеграций

Детализацию храни по одному файлу на сервис в `architecture/integrations/<service>.md`.
Название интеграции в формате `A -> B` уже описывает направление — дублировать в отдельных столбцах не нужно.

| Интеграция | Тип | Направление | Критичность | Детали |
| --- | --- | --- | --- | --- |
| `<сервис-источник> -> <сервис-назначение>` | `<REST\|gRPC\|Kafka\|SDK\|...>` | `<входящая\|исходящая\|двусторонняя>` | `<высокая\|средняя\|низкая>` | `[ссылка](./<service-file>.md)` |

## Разрез по направлению

### Входящие

- `<кто вызывает продукт и через какой интерфейс>`

### Исходящие

- `<какие внешние системы вызывает продукт>`

### Асинхронные

- `<брокеры, топики, очереди, вебхуки и событийные потоки>`

## Основные потоки данных

1. `<поток: источник -> продукт -> получатель, с ключевыми сущностями или событиями>`

## Аутентификация и границы доверия

- `<как выполняется аутентификация или авторизация, если это известно>`
- `<сетевые или защитные границы, если они видны в коде или конфигурации>`

## Примечания по надёжности

- `<таймауты, повторы, DLQ, идемпотентность, fallback, circuit breaker>`

## Наблюдаемость

- `<логи, метрики, трейсы, аудит или другие сигналы по интеграциям, если они найдены>`

## Пробелы и открытые вопросы

1. `<что выведено косвенно или требует подтверждения>`
