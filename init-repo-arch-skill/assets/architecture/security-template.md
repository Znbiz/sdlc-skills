---
title: "Модель аутентификации и границы доверия"
type: security
sources:
  - <repo-name/path/to/file>
related:
  - architecture/hld.md
created: "<YYYY-MM-DD>"
updated: "<YYYY-MM-DD>"
confidence: medium
domain: "<domain-id>"
repositories:
  - <repo-name>
---

# Модель аутентификации и границы доверия

## Аутентификация пользователей и внешних клиентов

| Точка входа | Механизм | Реализация | Тип утверждения | Источники |
| --- | --- | --- | --- | --- |
| `<endpoint / API>` | `JWT \| session \| API key \| OAuth2 \| mTLS \| SAML \| без аутентификации` | `<middleware, библиотека, сервис>` | `наблюдаемый факт \| выведено косвенно \| требует подтверждения` | `<repo-name/path/to/file>` |

## Межсервисное доверие

| От | К | Механизм | Тип утверждения | Источники |
| --- | --- | --- | --- | --- |
| `<service>` | `<service>` | `JWT \| mTLS \| shared secret \| service account \| без аутентификации` | `наблюдаемый факт \| выведено косвенно \| требует подтверждения` | `<repo-name/path/to/file>` |

## Границы доверия

- Внешний периметр: `<что находится снаружи периметра, что — внутри>`
- Внутренние зоны: `<если есть разграничение между группами внутренних сервисов>`
- Управление секретами: `<Vault / k8s secrets / env vars / hardcoded — указать, что наблюдалось>`
- Тип утверждения: `наблюдаемый факт | выведено косвенно | требует подтверждения`
- Источники:
  - `<repo-name/path/to/config-or-manifest>`

## Чувствительные данные

| Сущность / поле | Тип | Сервисы с доступом | Шифрование at rest | Шифрование in transit | Тип утверждения | Источники |
| --- | --- | --- | --- | --- | --- | --- |
| `<entity.field>` | `PII \| credentials \| financial \| health \| internal` | `<service-name>` | `да \| нет \| не определено` | `да \| нет \| не определено` | `наблюдаемый факт \| выведено косвенно \| требует подтверждения` | `<repo-name/path/to/file>` |

## Примечания по достоверности

- `<что подтверждено кодом или конфигом, что выведено косвенно, что требует уточнения>`

## Закрытые вопросы

- `Q-XXXX` — <какой вопрос был закрыт этим артефактом>
