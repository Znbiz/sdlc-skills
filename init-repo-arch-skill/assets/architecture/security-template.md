# Модель аутентификации и границы доверия

## Аутентификация пользователей и внешних клиентов

| Точка входа | Механизм | Реализация | Источник подтверждения |
| --- | --- | --- | --- |
| `<endpoint / API>` | `JWT \| session \| API key \| OAuth2 \| mTLS \| SAML \| без аутентификации` | `<middleware, библиотека, сервис>` | `<файл, конфиг>` |

## Межсервисное доверие

| От | К | Механизм | Источник подтверждения |
| --- | --- | --- | --- |
| `<service>` | `<service>` | `JWT \| mTLS \| shared secret \| service account \| без аутентификации` | `<файл, конфиг>` |

## Границы доверия

- Внешний периметр: `<что находится снаружи периметра, что — внутри>`
- Внутренние зоны: `<если есть разграничение между группами внутренних сервисов>`
- Управление секретами: `<Vault / k8s secrets / env vars / hardcoded — указать, что наблюдалось>`

## Чувствительные данные

| Сущность / поле | Тип | Сервисы с доступом | Шифрование at rest | Шифрование in transit | Источник подтверждения |
| --- | --- | --- | --- | --- | --- |
| `<entity.field>` | `PII \| credentials \| financial \| health \| internal` | `<service-name>` | `да \| нет \| не определено` | `да \| нет \| не определено` | `<файл, конфиг, миграция>` |

## Примечания по достоверности

- `<что подтверждено кодом или конфигом, что выведено косвенно, что требует уточнения>`
