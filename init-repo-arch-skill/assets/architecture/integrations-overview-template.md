# Обзор интеграций

## Границы описания

- Продукт или система: `<название>`
- Границы анализа: `<репозитории, сервисы, контур или среда при необходимости>`
- Уровень достоверности: `<высокий|средний|низкий>`

## Общая картина интеграций

<кратко опиши, какие внешние и межсервисные интеграции есть в системе и почему они важны>

## Карта интеграций

Детализацию храни по одному файлу на сервис.
Внутри service-файла перечисляй все входящие и исходящие интеграции этого сервиса.

| Сервис | Интеграция | Тип | Направление | Исходный компонент | Целевая система | Назначение | Критичность | Детали |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `<сервис>` | `<сервис-источник>-><сервис-назначение>` | `<REST|gRPC|Kafka|SFTP|DB link|SDK|...>` | `<входящая|исходящая|двусторонняя>` | `<сервис или джоба>` | `<система>` | `<для чего нужна>` | `<высокая|средняя|низкая>` | `[ссылка](./<service-file>.md)` |

## Разрез по направлению

### Входящие

- `<кто вызывает продукт и через какой интерфейс>`

### Исходящие

- `<какие внешние системы вызывает продукт>`

### Асинхронные

- `<брокеры, топики, очереди, вебхуки и событийные потоки>`

## Основные потоки данных

1. `<поток: источник -> продукт -> получатель, с ключевыми сущностями или событиями>`

## Аутентификация и границы доверия

- `<как выполняется аутентификация или авторизация, если это известно>`
- `<сетевые или защитные границы, если они видны в коде или конфигурации>`

## Примечания по надежности

- `<таймауты, повторы, DLQ, идемпотентность, fallback, circuit breaker>`

## Наблюдаемость

- `<логи, метрики, трейсы, аудит или другие сигналы по интеграциям, если они найдены>`

## Пробелы и неизвестные области

1. `<что выведено косвенно или требует подтверждения>`

## Подтверждающие артефакты

- `<файлы, конфигурации, схемы и участки кода, использованные как подтверждение>`
